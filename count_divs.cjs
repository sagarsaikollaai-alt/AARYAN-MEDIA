const fs = require('fs'); const code = fs.readFileSync('src/pages/CourseDetailsPage.tsx', 'utf8'); const lines = code.split(/\r?\n/); const start = lines.findIndex(l => l.includes('return (')); if(start === -1){ console.error('return not found'); process.exit(1);} let open=0, close=0; for(let i=start;i<lines.length;i++){ const line = lines[i]; const opens = (line.match(/<div(\s|>|$)/g) || []).length; const closes = (line.match(/<\/div>/g) || []).length; open += opens; close += closes; console.log(${i+1}: open+ close+ total open= close= ); } console.log('FINAL', open, close);
